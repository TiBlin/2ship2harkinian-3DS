#pragma once

// The native runtime owns its Fast3D interpreter directly.  Desktop editor
// declarations are intentionally unavailable in the vanilla 3DS build.
namespace Fast {
class Interpreter;
}
