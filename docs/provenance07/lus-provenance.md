# Audit LUS 07

Exact official baseline plus each complete final delta and SHA256. Compatibility-only declarations are explicitly distinguished from authored implementations. Matching API spellings do not establish authorship. No claim that upstream LUS is original Blinky.

Prior E/F entries: 58. Final records: {'B+A': 53, 'B': 3, 'retired': 2, 'A': 1}.

B+A means attributed official LUS plus an explicitly described native adaptation. For public_abi_adapter entries the change is mechanically imposed by public declarations; no claim of a newly authored implementation is made.

## Files

- `third_party/libultraship/cmake/dependencies/common.cmake` — **B+A**, rewritten_native_adapter. Native ImGui platform-source exclusion; verified bundled stb header without network/cache mutation; prism target inherits the selected native ARM ABI. Desktop dependency definitions remain official LUS.
- `third_party/libultraship/CMakeLists.txt` — **B+A**, rewritten_native_adapter. Pinned local source overrides retained; microcode option is a validated string checked against a supported list, replacing the inherited option-validation block.
- `third_party/libultraship/include/fast/backends/gfx_rendering_api.h` — **B+A**, preserved_blinky_adapter. TextureHasStorage is the Blinky residency contract: default true for existing backends, allows native texture DMA eviction without invalidating LUS cache handles. Interpreter call site retained.
- `third_party/libultraship/include/fast/Fast3dGui.h` — **B+A**, public_abi_adapter. GuiWindowInitData native width/height carrier is a public data contract required by existing BlinkyLusIntegration. No rendering implementation is present; desktop union layout is official LUS.
- `third_party/libultraship/include/fast/Fast3dWindow.h` — **B+A**, public_abi_adapter. Fixed backend discriminator 4 is retained for existing config and Blinky callers; KeyDown/KeyUp take the int callback parameter required by GfxWindowBackend. These declarative requirements do not imply authorship of LUS.
- `third_party/libultraship/include/libultraship/libultra/abi.h` — **B+A**, public_abi_adapter. Include canonical official types.h instead of redeclaring u32; removes inherited self-typedef.
- `third_party/libultraship/include/libultraship/libultra/controller.h` — **B+A**, public_abi_adapter. OS PIF fields use the official s32 typedef to agree with MM on devkitARM; reconstructed from pinned official header.
- `third_party/libultraship/include/libultraship/libultra/eeprom.h` — **B+A**, public_abi_adapter. EEPROM prototypes use s32 required by MM/libultra ABI; reconstructed from pinned official header.
- `third_party/libultraship/include/libultraship/libultra/gbi.h` — **B**, restored_upstream. Restored exact official gbi.h. Inherited numeric cast substitutions and MSVC self-alias macros were unnecessary on ARM.
- `third_party/libultraship/include/libultraship/libultra/gu.h` — **B+A**, public_abi_adapter. Hilite texture dimensions use s32 required by linked MM implementations; source expressions otherwise official.
- `third_party/libultraship/include/libultraship/libultra/interrupt.h` — **B+A**, public_abi_adapter. OSIntMask uses canonical u32 rather than distinct devkitARM uint32_t; header otherwise official.
- `third_party/libultraship/include/libultraship/libultra/message.h` — **B+A**, public_abi_adapter. Message queue prototype s32 names match MM and canonical types.h; no platform queue implementation in this header.
- `third_party/libultraship/include/libultraship/libultra/os.h` — **B+A**, public_abi_adapter. Canonical MM/libultra s32/u32 signatures and existing lus_osGetTime symbol separation from libctru clock retained. All mandatory declarations reconstructed from official header.
- `third_party/libultraship/include/libultraship/libultra/types.h` — **B**, restored_upstream. Exact official types.h restored. Prior alpha difference was commentary only.
- `third_party/libultraship/include/lus_3ds_controller.h` — **retired**, retired_unused. Unused duplicated controller header removed after recursive consumer search found no include or reference.
- `third_party/libultraship/include/ship/audio/CoreAudioAudioPlayer.h` — **B+A**, public_abi_adapter. Buffered() return type matches official AudioPlayer::Buffered int32_t; mandatory C++ override correction for platforms where int differs from int32_t.
- `third_party/libultraship/include/ship/audio/NullAudioPlayer.h` — **B+A**, public_abi_adapter. Buffered() return type matches official AudioPlayer::Buffered int32_t; mandatory override correction.
- `third_party/libultraship/include/ship/audio/SDLAudioPlayer.h` — **B+A**, public_abi_adapter. Buffered() return type matches official AudioPlayer::Buffered int32_t; mandatory override correction.
- `third_party/libultraship/include/ship/audio/WasapiAudioPlayer.h` — **B+A**, public_abi_adapter. Buffered() return type matches official AudioPlayer::Buffered int32_t; mandatory override correction.
- `third_party/libultraship/include/ship/config/ConsoleVariable.h` — **B+A**, rewritten_native_adapter. Inherited recursive mutable CVar synchronization replaced by one non-recursive publication mutex and immutable value ownership helpers. Public methods/signatures retained.
- `third_party/libultraship/include/ship/resource/archive/ArchiveManager.h` — **B+A**, preserved_blinky_adapter. Sorted compact archive index with borrowed stable archive path references replaces historical V7 reverse archive scan. Declaration matches tested Blinky ArchiveManager implementation.
- `third_party/libultraship/include/ship/resource/archive/O2rArchive.h` — **B+A**, preserved_blinky_adapter. NativeZip PIMPL exclusively selects independently authored BlinkyO2rArchive.cpp; official desktop libzip pool preserved.
- `third_party/libultraship/include/ship/resource/ResourceManager.h` — **B+A**, preserved_blinky_adapter. Resource publication helper and bounded cache-size API retained; removed accidentally duplicated ResourceManager helper declaration from unrelated ResourceIdentifier.
- `third_party/libultraship/include/ship/utils/transition_trace_3ds.h` — **retired**, retired_unused. Unused historical Soh3dsTransitionTrace weak-hook header removed after consumer search found no references.
- `third_party/libultraship/include/ship/window/gui/Gui.h` — **B+A**, preserved_blinky_adapter. Single FlushPendingSettings forwarding seam invokes existing official CheckSaveCvars without starting an ImGui render frame.
- `third_party/libultraship/src/CMakeLists.txt` — **B+A**, public_build_adapter. Official target definitions retain only public 32-bit controller ABI and conditional ENABLE_OPENGL exclusion for N3DS.
- `third_party/libultraship/src/fast/CMakeLists.txt` — **B+A**, rewritten_native_adapter. Exact unavailable backend filenames removed from native source list; all official source group construction remains unchanged.
- `third_party/libultraship/src/fast/Fast3dGui.cpp` — **B+A**, rewritten_native_adapter. Native ImGui presentation is compiled out, shared renderer ownership checked before texture access, typed shared ownership retained across GUI texture decoding. Inherited safety/comment blocks replaced; platform guards exclude unavailable SDL APIs.
- `third_party/libultraship/src/fast/Fast3dWindow.cpp` — **B+A**, rewritten_native_adapter. Native backend selected directly and constructed through existing Blinky factory. Blinky LCD viewport/frame/display-list/config-save integration retained; mandatory int callback ABI retained.
- `third_party/libultraship/src/fast/interpreter.cpp` — **B+A**, preserved_blinky_adapter. Only three narrow Blinky seams remain against official interpreter: omit dlfcn on 3DS, use native readable-address callback, check texture residency on cache hit. Entire historical renderer/interpreter customization was already removed and remains removed.
- `third_party/libultraship/src/fast/resource/factory/DisplayListFactory.cpp` — **B**, restored_upstream. Exact official DisplayListFactory restored, including ownership of copied PushShader strings and null attribute validation omitted by alpha3.
- `third_party/libultraship/src/fast/resource/factory/TextureFactory.cpp` — **B+A**, preserved_blinky_adapter. Independent bounded texture dimension and zero-copy payload validation retained. No such validation exists in alpha3; direct official-vs-final patch recorded.
- `third_party/libultraship/src/libultraship/controller/controldeck/ControlDeck.cpp` — **B+A**, public_build_adapter. Native input path omits SDL event pump/wheel poll; original LUS pad merge and input-blocking behavior preserved.
- `third_party/libultraship/src/libultraship/controller/controldevice/controller/Controller.cpp` — **B+A**, preserved_blinky_adapter. Port zero reads independently authored BlinkyInput snapshots into official OSContPad fields. LUS lag buffer/merge behavior preserved.
- `third_party/libultraship/src/libultraship/libultra/os.cpp` — **B+A**, public_abi_adapter. Official libultra implementation retained; only s32/u32 function spellings reconstructed to match canonical MM declarations.
- `third_party/libultraship/src/libultraship/libultra/os_cache.cpp` — **B+A**, public_abi_adapter. Official cache compatibility implementation retained with canonical s32 declarations.
- `third_party/libultraship/src/libultraship/libultra/os_eeprom.cpp` — **B+A**, public_abi_adapter. Official EEPROM implementation retained with canonical s32 declarations.
- `third_party/libultraship/src/libultraship/libultra/os_mesg.cpp` — **B+A**, rewritten_native_adapter. Official desktop queue body retained under !__3DS__; native queue implementation replaced by separate BlinkyMessageQueue.cpp. Historical MESG_GUARD implementation removed.
- `third_party/libultraship/src/libultraship/libultra/os_pi.cpp` — **B+A**, public_abi_adapter. Official PI implementation and existing official audio DMA range guard retained; canonical s32/u32 function declarations agree with MM.
- `third_party/libultraship/src/libultraship/libultra/os_vi.cpp` — **B+A**, public_abi_adapter. Official VI implementation retained; canonical u32 parameter declarations agree with MM.
- `third_party/libultraship/src/libultraship/libultra/os_vm.cpp` — **B+A**, public_abi_adapter. Official VM compatibility implementation retained; canonical s32/u32 parameter declarations agree with MM.
- `third_party/libultraship/src/libultraship/window/gui/InputEditorWindow.cpp` — **B+A**, public_abi_adapter. Nine ImGui integer editing locals use int, as required by official ImGui SliderInt/Combo int* parameters on devkitARM. Entire UI implementation remains attributed upstream; no originality claim for these forced type adaptations.
- `third_party/libultraship/src/ship/audio/Audio.cpp` — **B+A**, preserved_blinky_adapter. Existing Blinky NDSP backend factory mapping and stereo restriction preserved; no historical NdspAudioPlayer implementation retained.
- `third_party/libultraship/src/ship/audio/CoreAudioAudioPlayer.cpp` — **B+A**, public_abi_adapter. Buffered() definition uses mandatory int32_t override type; platform audio implementation remains official.
- `third_party/libultraship/src/ship/audio/NullAudioPlayer.cpp` — **B+A**, public_abi_adapter. Buffered() definition uses mandatory int32_t override type; official null-player backpressure behavior preserved.
- `third_party/libultraship/src/ship/audio/SDLAudioPlayer.cpp` — **B+A**, public_abi_adapter. Buffered() definition uses mandatory int32_t override type; platform audio implementation remains official.
- `third_party/libultraship/src/ship/audio/WasapiAudioPlayer.cpp` — **B+A**, public_abi_adapter. Buffered() definition uses mandatory int32_t override type; platform audio implementation remains official.
- `third_party/libultraship/src/ship/CMakeLists.txt` — **B+A**, preserved_blinky_adapter. Native build includes independently authored BlinkyLifecycle and excludes retired historical audio source if restored accidentally. Official source discovery otherwise preserved.
- `third_party/libultraship/src/ship/config/Config.cpp` — **B+A**, preserved_blinky_adapter. Blinky05 config transaction preserves closed temp file, backup/rollback/recovery, save serialization and None path handling. Official JSON config operations retained; crash28 tests cover the new save mechanism.
- `third_party/libultraship/src/ship/config/ConsoleVariable.cpp` — **B+A**, rewritten_native_adapter. Rebuilt from official ConsoleVariable with immutable shared publication, atomic register, snapshot save, safely owned string copies, type replacement, and shared lifetime across erase/reload. Removes historical recursive-lock/Peek/SetString mechanism; official JSON/legacy format parsing retained.
- `third_party/libultraship/src/ship/Context.cpp` — **B+A**, rewritten_native_adapter. Replaced historical native logging branch with direct synchronous stderr logger initialization before any async thread pool exists. Preserved independent partial-initialization teardown guards; official desktop InitLogging restored.
- `third_party/libultraship/src/ship/resource/archive/ArchiveManager.cpp` — **B+A**, preserved_blinky_adapter. Compact sorted archive/hash index, stable borrowed path references and priority deduplication retained; structurally distinct from alpha reverse scan, covered by archive tests.
- `third_party/libultraship/src/ship/resource/archive/O2rArchive.cpp` — **B+A**, public_build_adapter. Official O2rArchive.cpp disabled only for native build, where separately authored BlinkyO2rArchive.cpp implements its public API.
- `third_party/libultraship/src/ship/resource/ResourceLoader.cpp` — **B+A**, preserved_blinky_adapter. Bounded XML/JSON input, checked init metadata, independently rebuilt alias resolution and resource dispatch/error path retained. Alpha shortcut/counters/unwind probe are absent; exact upstream delta recorded.
- `third_party/libultraship/src/ship/resource/ResourceManager.cpp` — **B+A**, preserved_blinky_adapter. Independent cache publication/absence distinction, node extraction outside lock, owner-aware unload and bounded worker count retained. Historical V7 cache-report code and synchronous override are absent.
- `third_party/libultraship/src/ship/utils/binarytools/BinaryWriter.cpp` — **B+A**, public_abi_adapter. String wire length uses int32_t to select the official 32-bit BinaryWriter overload unambiguously on devkitARM; rest is official source.
- `third_party/libultraship/src/ship/utils/binarytools/MemoryStream.cpp` — **B+A**, preserved_blinky_adapter. Independent checked stream ranges, zero-length handling, non-consuming failure cursor, overflow-safe writes and accurate buffer size retained. Alpha file equals official and lacks these fixes.
- `third_party/libultraship/src/ship/window/gui/resource/GuiTextureFactory.cpp` — **B+A**, public_abi_adapter. stb_image output dimensions use int storage required by its public int* API, then copy to existing Metadata int32_t fields. This mechanical ABI adapter is attributed as compatibility work, not a new image decoder.
- `third_party/libultraship/src/libultraship/libultra/BlinkyMessageQueue.cpp` — **A**, new_implementation. Independent bounded native message queue implementation using canonical public queue layout, serialized operations, no modulo overflow, correct prepend on empty/full queue, checked event index and initialization. 100000 concurrent transfers plus ARM compilation verified.

## Validation

- Native message queues: real TU host tests, 100000 concurrent transfers; native ARM object compile.
- ConsoleVariable: real TU with fake persistence collaborator, ownership/alias/type/JSON/erase/reload and 100000 concurrent writes.
- Production Context teardown: 8 initialized/partial states.
- 30 maintained resource/texture/stream/cache/XML/alias regressions; host functional mode, no sanitizers.

Full official-to-final deltas and exact final file SHA256 values are embedded in lus-provenance.json.
The claim is a fully accounted-for native adaptation boundary, never a 100% original LUS library.
